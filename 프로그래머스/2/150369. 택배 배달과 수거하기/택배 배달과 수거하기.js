function solution(cap, n, deliveries, pickups) {
  
    let answer = 0;
    let have_to_deli = 0;
    let have_to_pick = 0;
    
    for(let i = n - 1; i >= 0; i--){
        have_to_deli -= cap;
        
    }

}